---
topic: "置换环"
category: "16-tricks"
source: "Tricks 经验积累.md"
---

# 置换环

简单来讲就是

对于一个数组 $[1,\ 2,\ 3,\ 4, \dots, n]$

这里的置换环数量就是 $n$，每个元素都是自己的一个自环

数组 $p$ $=$ $[3,\ 2,\ 4,\ 1]$

置换环数量就是 2，因为 $a_1$ → $3$, $a_3$ → $1$, $a_4$ → $4$, $a_4$ → $1$.

结论就是使得一个序列变为单位序列的交换元素次数就等于 $n - c$（置换环数量）

***
